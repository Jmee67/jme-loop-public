# Spec: TICKET-001

The project contains a minimal loop-ready ticket with repo-relative spec and plan
pointers.

## Acceptance Criteria

- `docs/project/context.md` exists.
- The epic has one ticket with `status: planned` and `loop: true`.
- The ticket points to this spec and its implementation plan.
