# Example Project Context

This sample project demonstrates the markdown shape that jme-loop expects. Replace this
file with your product goals, constraints, verification command, and house conventions.

## Verification

Run `npm test` before closing a ticket.
